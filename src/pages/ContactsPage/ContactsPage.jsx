import React from 'react';
import Contacts from '../../components/Contact/Contact';

const ContactsPage = () => {
    return (
        <div>
            <h1>Contacts</h1>
            <Contacts />
        </div>
    );
};

export default ContactsPage;